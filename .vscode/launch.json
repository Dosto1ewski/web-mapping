{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Standort Functions",
      "type": "coreclr",
      "request": "attach",
      "processId": "${command:azureFunctions.pickProcess}",
      "preLaunchTask": "func: host start"
    }
  ]
}
